version: '3.8'

services:

  # 1. Backend Service
  backend:
    build: ./backend
    container_name: mern_backend
    ports:
      - "5000:5000"
    # Yeh command aapki .env file ko safely container ke andar inject kar degi
    environment:
      - PORT=5000
      - MONGO_URI=mongodb+srv://Kumail2006:Kumail2006@portfolio-mern.wgijhho.mongodb.net/portfolio?appName=portfolio-mern
      - ADMIN_PASSWORD=2406
      - JWT_SECRET=super_secret_portfolio_key_123
      - CLOUDINARY_CLOUD_NAME=diqf2zsb1
      - CLOUDINARY_API_KEY=672725143743693
      - CLOUDINARY_API_SECRET=lt2Qkswnv_xcAIjEXwIyPz1qJR4

  # 2. Frontend Service
  frontend:
    build: ./frontend
    container_name: mern_frontend
    ports:
      - "5173:5173"
    depends_on:
      - backend